# Django on Kubernetes — End-to-End Example

This example demonstrates:

- Dual-layer onboarding contract (brief + spec)
- Phased, idempotent execution
- Kubernetes-first platform bootstrap
- Future extension to CI/CD and app deploy

Start by validating the spec:

```bash
make spec-validate